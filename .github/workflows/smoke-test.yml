name: Smoke tests
on:
  push:
    paths:
      - ".github/workflows/smoke-test.yml"
      - "Casks/*.rb"
      - "Formula/*.rb"
      - "Tests/**"
  schedule:
    - cron: "30 16 2 * *"
  workflow_dispatch:
    inputs:
      app:
        description: App name to test, or "all"
        required: true
        default: all
        type: string
permissions:
  contents: read
jobs:
  detect:
    name: Detect smoke-test targets
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.targets.outputs.apps }}
      count: ${{ steps.targets.outputs.count }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect targets
        id: targets
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          CURRENT_SHA: ${{ github.sha }}
          REQUESTED_APP: ${{ inputs.app }}
        run: |
          case "$EVENT_NAME" in
            push)
              mode=changed
              ;;
            workflow_dispatch)
              if [[ "$REQUESTED_APP" == "all" ]]; then
                mode=all
              else
                mode=app
              fi
              ;;
            schedule)
              mode=all
              ;;
            *)
              echo "Unsupported event: $EVENT_NAME" >&2
              exit 1
              ;;
          esac

          args=(--mode "$mode")
          if [[ "$mode" == "changed" ]]; then
            args+=(--base "$BEFORE_SHA" --head "$CURRENT_SHA")
          elif [[ "$mode" == "app" ]]; then
            args+=(--app "$REQUESTED_APP")
          fi

          apps="$(bash Tests/detect-targets.sh "${args[@]}")"
          if [[ "$apps" == "[]" ]]; then
            count=0
          else
            separators="${apps//[^,]/}"
            count=$((${#separators} + 1))
          fi
          echo "apps=$apps" >> "$GITHUB_OUTPUT"
          echo "count=$count" >> "$GITHUB_OUTPUT"
  smoke-test:
    name: ${{ matrix.app }} (${{ matrix.os }})
    needs: detect
    if: needs.detect.outputs.count != '0'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJSON(needs.detect.outputs.apps) }}
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Enable Homebrew on Linux
        if: runner.os == 'Linux'
        run: |
          echo "/home/linuxbrew/.linuxbrew/bin" >> "$GITHUB_PATH"
          echo "/home/linuxbrew/.linuxbrew/sbin" >> "$GITHUB_PATH"
      - name: Install ShellSpec
        env:
          HOMEBREW_NO_ANALYTICS: 1
          HOMEBREW_NO_AUTO_UPDATE: 1
          HOMEBREW_REQUIRE_TAP_TRUST: 1
        run: |
          brew trust --tap azure/bicep
          brew trust --tap shellspec/shellspec

          aws_tap="$(brew --repository aws/tap)"
          if [[ -d "$aws_tap" ]]; then
            for formula in aws-sam-cli aws-sam-cli-nightly; do
              if brew list --formula "$formula" >/dev/null 2>&1; then
                brew uninstall --force "$formula"
              fi
            done
            brew untap --force aws/tap
          fi

          hashicorp_tap="$(brew --repository hashicorp/tap)"
          if [[ -d "$hashicorp_tap" ]]; then
            if brew list --formula packer >/dev/null 2>&1; then
              brew uninstall --force packer
            fi
            brew untap --force hashicorp/tap
          fi

          brew install shellspec/shellspec/shellspec
          shellspec -v
      - name: Install and smoke-test app
        env:
          HOMEBREW_NO_ANALYTICS: 1
          HOMEBREW_NO_AUTO_UPDATE: 1
          HOMEBREW_REQUIRE_TAP_TRUST: 1
        run: |
          brew trust --tap KEINOS/apps
          bash Tests/run-smoke-test.sh "${{ matrix.app }}"
      - name: Verify generated definitions are unchanged
        if: always()
        run: git diff --exit-code -- Casks Formula
